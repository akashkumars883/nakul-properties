import property from './property';
import faq from './faq';
import post from './post';
import keywordPage from './keywordPage';

export const schema = {
  types: [property, faq, post, keywordPage],
}
